import {React} from 'react';
import { Route } from 'react-router-dom';

const Form = () => {
    return(
        <Route path='/pizza'>
          <form>
          </form>
        </Route>
    );
}

export default Form;